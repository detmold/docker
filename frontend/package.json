{
  "name": "frontend-demo",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "build": "mkdir -p dist && cp -r src/* dist/ || true",
    "start": "node -e \"console.log('Static frontend \u2014 serve dist with any static server')\""
  }
}