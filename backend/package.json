{
  "name": "backend-demo",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "build": "mkdir -p dist && cp -r src/* dist/ || true",
    "start": "node dist/main.js"
  },
  "dependencies": {
    "express": "^4.18.2"
  }
}